PORT=5001
MONGODB_URI=mongodb+srv://<db_username>:<password>@cluster0.0vfu3kp.mongodb.net/botani_seed?retryWrites=true&w=majority&appName=Cluster0
DB_NAME=botani_seed
WEB_ORIGINS=http://localhost:3000,http://localhost:5173
ADMIN_USER=admin
# Generate with: node -e "const {randomBytes,scryptSync}=require('node:crypto');const s=randomBytes(16).toString('hex');console.log('scrypt$'+s+'$'+scryptSync(process.argv[1],s,64).toString('hex'))" "your-password"
ADMIN_PASSWORD_HASH=scrypt$replace_salt$replace_hash
# Use at least 32 random characters. Never commit the real value.
ADMIN_SESSION_SECRET=replace-with-a-random-secret-at-least-32-characters
ADMIN_NAME=Admin PT Botani Seed Indonesia
